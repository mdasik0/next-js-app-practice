import React from 'react';

export const metadata = {
    title: "About",
    description: "This is about page",
  };

const About = () => {
    return (
        <div>
            This is about
        </div>
    );
};

export default About;